Ты строго следуешь моим инструкциям. Ты не интерпретируешь, не дополняешь и не додумываешь данные. Твоя задача — превратить мой текст в JSON для массового создания задач в JIRA.

ЖЁСТКИЕ ПРАВИЛА (нарушать запрещено):
1. Не добавляй задачи, которых нет в моём тексте. Сколько задач я перечислил — столько и должно быть в поле "tasks".
2. Не разбивай одну мою задачу на несколько и не объединяй разные задачи в одну.
3. Поля "summary" и "description" формируй только из моего текста. Не расширяй, не перефразируй, не добавляй детали и контекст, которых у меня нет.
4. Если поле ("priority", "epic_link", "components", "labels", "assignee", "issuetype") не указано в моём тексте явно — не включай это поле в JSON задачи. Ничего не подставляй по умолчанию, ничего не угадывай.

ПРАВИЛА ЗАПОЛНЕНИЯ ПОЛЕЙ:
- "components": "Frontend" или "Backend" — только если я явно сказал "фронт"/"frontend" или "бэк"/"backend". Иначе поле отсутствует.
- "issuetype": "Задача" или "Ошибка" — только если я явно указал тип. Иначе поле отсутствует.
- "summary": краткое описание задачи. Если я указал раздел — добавь его в начале (например, "AUTH: ..."). Если указан компонент — добавь "ФРОНТЕНД" или "БЭКЕНД" в конце. Без указаний — просто краткое описание.
- "description": подробное описание из моего текста. Подразделы "Шаги воспроизведения" и "Ожидаемый результат" добавляй только если я их явно описал.
- "epic_link": заполняй только если я явно указал ключ эпика. Не угадывай.
- "priority": "Highest" | "Medium" | "Low" — только если я явно указал приоритет. Не выбирай сам.

Верни ровно структуру из примера ниже, заполнив её моими данными. Не добавляй новых полей и не убирай существующие из "jira_config".

Пример JSON:
{
  "jira_config": {
    "project_key": "SO"
  },
  "tasks": [
    {
      "summary": "ДОКУМЕНТЫ: Прогрузка локальных документов БЭКЕНД",
      "description": "В разделе «Локальные документы», ",
      "issuetype": "Задача",
      "priority": "Highest",
      "components": ["Backend"],
      "epic_link": "SO-4327"
    }
  ]
}

вот задачи:
